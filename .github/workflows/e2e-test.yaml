name: "End-to-end Tests"
on:
  workflow_dispatch:
    inputs:
      realtime_flakiness_runs:
        description: "Repeat RealtimeApiTest#testSpeechToSpeech N times (0 = skip)"
        required: false
        default: "0"
  schedule:
    - cron: 15 2 * * MON-FRI

env:
  MVN_MULTI_THREADED_ARGS: --batch-mode --no-transfer-progress --fail-at-end --show-version --threads 1C
  JAVA_VERSION: 21

jobs:
  end-to-end-tests:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:

      - name: "Checkout repository"
        uses: actions/checkout@v7

      - name: "Setup java"
        uses: actions/setup-java@v5.7.0
        with:
          distribution: "sapmachine"
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'maven'

      - name: "Build SDK"
        run: mvn ${{ env.MVN_MULTI_THREADED_ARGS }} clean install -DskipTests -DskipFormatting

      - name: "Run RealtimeApiTest#testSpeechToSpeech (100x)"
        env:
          AICORE_SERVICE_KEY: ${{ secrets.AI_CORE_CANARY }}
        run: |
          failures=0
          for i in $(seq 1 100); do
            echo "--- Run $i / 100 ---"
            if ! mvn --batch-mode --no-transfer-progress surefire:test \
                -pl :spring-app \
                -DskipTests=false \
                "-Dtest=com.sap.ai.sdk.app.controllers.RealtimeApiTest#testSpeechToSpeech" \
                "-Daicore.landscape=canary"; then
              failures=$((failures + 1))
              echo "Run $i FAILED"
            fi
          done
          echo "Failures: $failures / 100"
          [ $failures -eq 0 ]
